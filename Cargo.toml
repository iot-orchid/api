[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7.5"
serde = {version = "1.0.208", features = ["derive"]}
tokio = {version = "1.39.3", features = ["full"]}
tower = "0.5.0"